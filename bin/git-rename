#!/bin/bash
CURRENT_BRANCH_NAME=`git rev-parse --abbrev-ref HEAD`
NEW_BRANCH_NAME=$1

if [ NEW_BRANCH_NAME == '' ]; then
    echo 'command that you can rename a branch of git'
    echo 'git rename CURRENT_BRANCH_NAM ENEW_BRANCH_NAME'
fi

git branch -m $CURRENT_BRANCH_NAME $NEW_BRANCH_NAME
